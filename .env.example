# Frontend Only Configuration
# No backend or API keys needed - all data is stored locally

# Optional: If you want to integrate with external APIs in the future
# NEXT_PUBLIC_API_URL=https://your-api-url.com
